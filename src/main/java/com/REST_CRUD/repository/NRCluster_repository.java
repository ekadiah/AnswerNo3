package com.REST_CRUD.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.REST_CRUD.model.NR_Cluster;

public interface NRCluster_repository extends JpaRepository<NR_Cluster, Integer>{

}
